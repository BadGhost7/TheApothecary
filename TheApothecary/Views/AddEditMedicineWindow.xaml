<Window x:Class="TheApothecary.Views.AddEditMedicineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить лекарство" Height="646" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
 

    <Window.Resources>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

       
        <TextBlock Grid.Row="0" Text="{Binding WindowTitle}" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"
                   Foreground="#2C3E50"/>

        
        <TextBlock Grid.Row="1" Text="Название лекарства:" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="NameTextBox" Grid.Row="2" Style="{StaticResource TextBoxStyle}"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

       
        <TextBlock Grid.Row="3" Text="Описание:" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="4" Style="{StaticResource TextBoxStyle}"
                 TextWrapping="Wrap" AcceptsReturn="True" Height="80"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>

      
        <TextBlock Grid.Row="5" Text="Цена (₽):" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="PriceTextBox" Grid.Row="6" Style="{StaticResource TextBoxStyle}"
                 Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>

        
        <TextBlock Grid.Row="7" Text="Количество на складе:" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="QuantityTextBox" Grid.Row="8" Style="{StaticResource TextBoxStyle}"
                 Text="{Binding StockQuantity, UpdateSourceTrigger=PropertyChanged}"/>

        
        <TextBlock Grid.Row="9" Text="Категория:" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="CategoryTextBox" Grid.Row="10" Style="{StaticResource TextBoxStyle}"
                 Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"/>

       
        <CheckBox x:Name="PrescriptionCheckBox" Grid.Row="11" 
                  Content="Требуется рецепт" Margin="0,10,0,20"
                  IsChecked="{Binding RequiresPrescription}"
                  FontWeight="SemiBold" Foreground="#2C3E50"/>

       
        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,15" Grid.RowSpan="2">
            <Button x:Name="SaveButton" Content="Сохранить" 
                    Style="{StaticResource ButtonStyle}"
                    Background="#3498DB" Foreground="White"
                    Click="SaveButton_Click"/>

            <Button x:Name="CancelButton" Content="Отмена" 
                    Style="{StaticResource ButtonStyle}"
                    Background="#95A5A6" Foreground="White"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>