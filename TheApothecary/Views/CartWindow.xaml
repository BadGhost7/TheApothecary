<Window x:Class="TheApothecary.Views.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TheApothecary"
        Title="Корзина - The Apothecary" 
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

      
    </Window.Resources>

    <Border BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,20">
                <TextBlock Text="The Apothecary" 
                          FontSize="24" FontWeight="Bold" 
                          HorizontalAlignment="Center"
                          Foreground="#2C3E50"/>
                <TextBlock Text="Корзина покупок" 
                          FontSize="18" FontWeight="SemiBold"
                          HorizontalAlignment="Center"
                          Foreground="#34495E"/>
            </StackPanel>

            <!-- Предупреждение о рецептах -->
            <Border Grid.Row="1" 
                    x:Name="PrescriptionWarningBorder"
                    Background="#FFF8E1" 
                    BorderBrush="#FFA000" 
                    BorderThickness="1"
                    Padding="15"
                    Margin="0,0,0,15"
                    CornerRadius="4"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚠️" FontSize="16" Margin="0,0,10,0"/>
                    <TextBlock x:Name="PrescriptionWarningText"
                              Text="Некоторые товары требуют проверки рецепта сотрудником" 
                              FontSize="14" Foreground="#E65100"
                              FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- Список товаров в корзине -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                <ItemsControl x:Name="CartItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="15,12" Background="White">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Информация о товаре -->
                                    <StackPanel Grid.Column="0" Orientation="Vertical">
                                        <TextBlock Text="{Binding Medicine.Name}" 
                                                   FontSize="14" FontWeight="SemiBold"
                                                   Foreground="#2C3E50"/>
                                        <TextBlock Text="{Binding Medicine.Description}" 
                                                   FontSize="12" Foreground="#7F8C8D"
                                                   TextWrapping="Wrap" MaxWidth="300"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="Рецепт: " 
                                                       FontSize="11" Foreground="#95A5A6"/>
                                            

                                            <TextBlock Text=" • Статус: " 
                                                       FontSize="11" Foreground="#95A5A6" Margin="10,0,0,0"/>
                                            <TextBlock Text="{Binding PrescriptionStatusText}" 
                                                       FontSize="11"
                                                       Foreground="{Binding PrescriptionStatusColor}"/>
                                        </StackPanel>

                                        <!-- Кнопка запроса проверки рецепта -->
                                        <Button Content="📋 Запросить проверку рецепта" 
                                                Click="RequestPrescriptionCheck_Click"
                                                Tag="{Binding Medicine.Id}"
                                                Visibility="{Binding ShowRequestButton, Converter={StaticResource BoolToVis}}"
                                                Background="#F39C12"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Padding="10,5"
                                                Margin="0,10,0,0"
                                                FontSize="11"
                                                Cursor="Hand"
                                                HorizontalAlignment="Left"
                                                ToolTip="Отправить запрос на проверку рецепта сотруднику"/>
                                    </StackPanel>

                                    <!-- Количество -->
                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="20,0">
                                        <TextBlock Text="Кол-во" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Quantity}" 
                                                   FontSize="14" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"
                                                   Foreground="#2C3E50"/>
                                    </StackPanel>

                                    <!-- Цена за единицу -->
                                    <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="20,0">
                                        <TextBlock Text="Цена" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Price}" 
                                                   FontSize="14" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"
                                                   Foreground="#27AE60"/>
                                    </StackPanel>

                                    <!-- Сумма -->
                                    <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Center" Margin="20,0">
                                        <TextBlock Text="Сумма" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding TotalPrice}" 
                                                   FontSize="14" FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   Foreground="#2C3E50"/>
                                    </StackPanel>

                                    <!-- Кнопки управления -->
                                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="❌" 
                                                Click="RemoveItem_Click"
                                                Tag="{Binding Medicine.Id}"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="8"
                                                FontSize="12"
                                                Cursor="Hand"
                                                ToolTip="Удалить из корзины"/>
                                        <!-- Убрал старую кнопку VerifyPrescription_Click -->
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Итого и кнопки -->
            <Border Grid.Row="3" 
                    Background="#F8F9FA" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1"
                    Padding="20"
                    Margin="0,0,0,15"
                    CornerRadius="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               x:Name="TotalText"
                               Text="Итого: 0₽ (товаров: 0)" 
                               FontSize="16" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Закрыть" 
                                Click="CloseButton_Click"
                                Background="Transparent"
                                Foreground="#7F8C8D"
                                BorderBrush="#7F8C8D"
                                BorderThickness="1"
                                Padding="15,8"
                                Margin="0,0,10,0"
                                FontSize="14"/>

                        <Button Content="Оформить заказ" 
                                x:Name="CheckoutButton"
                                Click="CheckoutButton_Click"
                                Background="#3498DB"
                                Foreground="White"
                                BorderThickness="0"
                                Padding="20,8"
                                FontSize="14"
                                FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>